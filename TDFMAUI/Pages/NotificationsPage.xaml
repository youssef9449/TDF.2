<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:TDFMAUI.ViewModels"
             x:Class="TDFMAUI.Pages.NotificationsPage"
             x:DataType="viewmodels:NotificationsViewModel"
             Title="{Binding Title}">

    <Grid RowDefinitions="Auto,*,Auto" Padding="10">
        <Button Grid.Row="0" Text="Mark All Read" Command="{Binding MarkAllReadCommand}" HorizontalOptions="End" Margin="0,0,0,10" />

        <RefreshView Grid.Row="1" Command="{Binding LoadNotificationsCommand}" IsRefreshing="{Binding IsBusy}">
            <CollectionView x:Name="notificationsCollection" ItemsSource="{Binding Notifications}" EmptyView="No notifications">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="viewmodels:NotificationItemViewModel">
                        <Frame BackgroundColor="{Binding BackgroundColor}" Margin="0,5" Padding="10">
                            <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto">
                                <Label Grid.Row="0" Grid.Column="0" Text="{Binding SenderName}" FontAttributes="Bold" />
                                <Label Grid.Row="0" Grid.Column="1" Text="{Binding TimestampFormatted}" FontSize="10" TextColor="Gray" HorizontalOptions="End" />
                                <Label Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding Message}" Margin="0,5,0,0" />

                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding BindingContext.ViewNotificationCommand, Source={x:Reference notificationsCollection}}" CommandParameter="{Binding .}" />
                                </Grid.GestureRecognizers>
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </RefreshView>

        <ActivityIndicator Grid.Row="1" IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}" Color="{StaticResource Primary}" HorizontalOptions="Center" VerticalOptions="Center" />
    </Grid>
</ContentPage>
